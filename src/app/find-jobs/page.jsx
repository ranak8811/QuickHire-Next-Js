import JobsList from "./JobsList";

export const metadata = {
  title: "Find Jobs | QuickHire",
  description: "Browse and search for the latest job opportunities on QuickHire.",
};

const FindJobs = () => {
  return <JobsList />;
};

export default FindJobs;
