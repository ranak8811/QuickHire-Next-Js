import LoginForm from "./LoginForm";

export const metadata = {
  title: "Login | QuickHire",
  description: "Login to your QuickHire account to manage your applications or job listings.",
};

const LoginPage = () => {
  return <LoginForm />;
};

export default LoginPage;
