import RegisterForm from "./RegisterForm";

export const metadata = {
  title: "Register | QuickHire",
  description: "Create a QuickHire account to start applying for jobs or posting listings.",
};

const RegisterPage = () => {
  return <RegisterForm />;
};

export default RegisterPage;
